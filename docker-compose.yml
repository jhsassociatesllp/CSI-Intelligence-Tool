services:
  compliance-app:
    build: .
    container_name: compliance_app
    ports:
      - "${APP_PORT}:9040"
    environment:
      - "${APP_PORT}:9040"
      - MONGODB_URL=${MONGODB_URL}
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: always
    volumes:
      - ./uploads:/app/uploads